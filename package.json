{
  "name": "repotrail",
  "displayName": "RepoTrail",
  "description": "Capture and recover context from code investigations.",
  "version": "0.0.1",
  "publisher": "repotrail",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tahamoeini/repotrail.git"
  },
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "RepoTrail",
      "properties": {
        "repotrail.capture.retentionMinutes": {
          "type": "number",
          "default": 20,
          "minimum": 1,
          "maximum": 240,
          "description": "How long RepoTrail keeps rolling observed activity in memory before it can be saved retroactively."
        },
        "repotrail.capture.maxEvents": {
          "type": "number",
          "default": 1000,
          "minimum": 100,
          "maximum": 20000,
          "description": "Maximum observed events retained per workspace in the rolling in-memory buffer."
        },
        "repotrail.lifecycle.autoSaveSeconds": {
          "type": "number",
          "default": 15,
          "minimum": 0,
          "maximum": 3600,
          "description": "Debounce interval for automatic active-investigation persistence. Set to 0 to disable autosave between explicit save points."
        }
      }
    },
    "commands": [
      {
        "command": "repotrail.startInvestigation",
        "title": "Start Investigation",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.saveRecentActivityAsInvestigation",
        "title": "Save Recent Activity as Investigation",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.updateCheckpoint",
        "title": "Add or Update Checkpoint",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.attachBrowserReference",
        "title": "Attach Current Page to RepoTrail",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.saveAndStopInvestigation",
        "title": "Save and Stop Investigation",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.listInvestigations",
        "title": "List Saved Investigations",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.openResumeSnapshot",
        "title": "Show Resume Snapshot",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.resumeInvestigation",
        "title": "Resume Investigation",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.deleteInvestigation",
        "title": "Delete Investigation",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.deleteAllData",
        "title": "Delete All RepoTrail Data",
        "category": "RepoTrail"
      },
      {
        "command": "repotrail.showStorageLocation",
        "title": "Show Local Storage Location",
        "category": "RepoTrail"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vscode-test",
    "test:unit": "mocha out/test/domain.test.js out/test/storage.test.js out/test/capture.test.js out/test/git.test.js out/test/investigationLifecycle.test.js out/test/resumeSnapshot.test.js out/test/resumeAction.test.js --ui tdd",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.100.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.0",
    "@vscode/vsce": "^3.0.0",
    "eslint": "^9.0.0",
    "mocha": "^12.0.0",
    "typescript": "^5.7.0"
  }
}
